## What this changes

<!-- One or two sentences. What does a user of the package see that they didn't before? -->

## Why

<!-- The problem, not the patch. Link the issue if there is one. -->

## Checklist

- [ ] `npm run lint && npm run typecheck && npm test` pass
- [ ] A changeset is included (`npm run changeset`), or this change does not
      affect the published package (CI, docs, demo)
- [ ] The scope stays inside what `AGENTS.md` says the package is for
